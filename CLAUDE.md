* このCLAUDE.mdが置かれているディレクトリパスを、ルートディレクトリとし、特に断りのない限り、全ての指示はこのディレクトリからの相対パスで記述される
* 実装にはGoを使用すること
* ルートディレクトリにMakefileを置き、ビルドルールを記述する。必ずこのMakefileを使用してビルドすること
* ビルド済みのプログラムは、build/ディレクトリに配置し、テスト等でもこのディレクトリを参照すること
* テストは、tests/ディレクトリに配置し、そこで実行すること
